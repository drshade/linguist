[package]
name = "linguist-types"
version = "0.1.1"
edition = "2024"

description = "Type definitions for linguist language detection library"
license = "MIT"
repository = "https://github.com/drshade/linguist"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
